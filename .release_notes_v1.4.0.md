コードベース全体の監査(2026-09-08)で洗い出した改善項目21件をすべて反映した
リリースです。ウォーターフォール表示中の操作ずれをはじめとする**バグ修正6件**、
**新機能3件**、プロパティパネル・メニューの整理、描画とテストの高速化を含みます。

### 新機能

- **よく使う色を名前付きで登録できるようになりました。** 「試料A」のように名前を
  付けて色を登録しておき、データセットの色欄のスウォッチから名前で選べます。
  複数のデータセットを選んでまとめて適用することもでき(Undo 1回で戻せます)、
  登録はプロジェクトをまたいで保持されるため、別の図でも同じ物質に同じ色を
  使えます。登録が増えたときは検索欄付きの一覧から選べます。
  なお**色欄のスウォッチを押すとメニューが開くようになりました**。従来どおりの
  自由な色選択は、そのメニューの「その他の色...」から行えます。
- **列の値で系列を自動分割できるようになりました。** 「試料名」「条件」のような
  区分列を持つ1つのファイルから、その値ごとに別々の系列を一括で作成します
  (右クリック ▸ データ処理 ▸ 「列の値で系列に分割...」)。色はアクティブな
  パレットから順に割り当てられ、元のデータセットは残ります。
- **3列目の値で点を色分けできるようになりました。** プロット種別に
  「Z-Color Scatter」を追加しました。温度・時間・濃度などの列を選ぶと、その値に
  応じてカラーマップで点が着色され、カラーバーも表示されます。

### バグ修正

- **ウォーターフォール表示中に、クリックやドラッグの位置がずれる問題を修正しました。**
  積み重ねた2本目以降のトレースに対して、範囲選択によるマスク・データカーソルの
  読み取り値・データエディタ連動のハイライト・多峰フィットのピーク配置が、
  いずれもオフセット分ずれた位置で処理されていました。
- **データセットの削除が元に戻せるようになりました。** 従来は削除するとデータ・
  スタイル・フィット結果・マスクがまとめて復旧不能でした。フォルダ内での位置も
  含めて復元します。
- **プロジェクトを開いた直後に「元に戻す」を実行すると、読み込んだデータセットが
  以前のものに置き換わる問題を修正しました。**
- **注釈モード・データカーソルモードが、自由配置編集モードを解除しない問題を
  修正しました。** 両方が同時に有効になり、キャンバス上のドラッグが二重に反応して
  いました。
- **ウォーターフォール表示で、トレースを1本非表示にすると後ろが1段ずつ繰り上がる
  問題を修正しました。** 非表示にした段は空いたまま保持されます。
- **2Dマップ(ヒートマップ)で、効果のないウォーターフォール設定が表示されたままに
  なっていたのを修正しました。**
- **自動スケールを解除して保存した軸の範囲が、プロジェクトを開き直したり
  サブプロットを切り替えたりするたびに失われる問題を修正しました。**
- **統計値ラベルが、非表示にしただけのデータセットに対して「(データセットなし)」と
  出力される問題を修正しました。**
- **コマンドパレットの候補に「(履歴なし)」が混入することがある問題を修正しました。**

### 操作性の改善

- **データセットのプロパティパネルを7つの折りたたみセクションに整理しました**
  (データ列 / 基本スタイル / グラデーション / ウォーターフォール /
  2Dマップ・値による配色 / 表示の追加要素 / 配置・情報)。従来は39行が1列に並び、
  プロット種別を切り替えるだけでパネルが大きく伸縮していました。開閉の状態は
  記憶されます。
- **データセットの右クリックメニューを5つのサブメニューに整理しました**
  (約30項目 → トップレベル12項目)。同じメニューは**メニューバーの
  「データセット」からも開けます**。
- **ファイルメニューを整理しました。** エクスポート系7項目を「エクスポート ▶」に
  まとめ(22項目 → 15項目)、先頭に「データファイルを開く」を追加しました。
  色覚シミュレーションプレビューは表示メニューへ移しました。
- **入力欄の左端が揃うようになりました。** X軸タブとY軸タブ、プロパティパネルの
  各セクションで、ラベル列の幅が微妙に違っていました。
- **ボタンを押したときの色を青に統一しました。** 一部に緑(ティール)が残って
  いました。スクロールバーのハイライトは、選択表現と紛れないよう濃いグレーに
  しています。

### 性能

- **インセット(拡大図)を置いたときの再描画が速くなりました。** 拡大図の中の
  描画が本体と同じ間引きを通るようになり、20万点+インセット1つで
  0.349秒 → 0.199秒になりました。
- **軸の書式を変更したときの再描画が速くなりました。** 内容が変わっていない注釈を
  作り直さないようにしたことで、注釈の再生成にかかる時間が
  8.6ミリ秒 → 0.01ミリ秒(インセット1つ、20万点)になりました。

### 内部の変更(動作に影響はありません)

- プラグインは種類ごとに別リポジトリで開発する方針に変更しました。本体に残るのは
  APIの使い方を示す同梱サンプルのみです。配布用zipのビルドスクリプトを追加しました。
- 7つのマウスモードの排他制御を1つの登録簿に集約しました(7ファイルに散っていた
  42分岐を置き換え)。
- `gui/dialogs.py`(5,560行・47ダイアログ)を機能群ごとの6モジュールに分割しました。
- テストスイートの実行時間を**約35分から約17.5分**に短縮しました。テストが閉じずに
  残したウィンドウが蓄積し、後半のテストほど遅くなっていたのが主因でした。
- テストを2,705件から2,920件に増やしました。

### ライセンス

- **`LICENSE`(MIT)と `THIRD_PARTY_LICENSES.md` を追加しました。** 同梱している
  Qt/PySide6 が LGPL v3 であることを含め、再配布時の条件を明記しています。

---

## ダウンロード

| OS | ファイル | 備考 |
|---|---|---|
| Windows | `Graphica-windows.zip` | 展開して `Graphica.exe` を実行 |
| macOS | `Graphica-macos.zip` | Apple Silicon (arm64) 向け。**展開すると中に `Graphica-app.zip` があるので、もう一度展開してください**(次回リリースから1段になります) |

**macOS は署名していないため、初回は右クリック(またはControlキーを押しながら
クリック)して「開く」を選んでください。** ダブルクリックだけでは Gatekeeper に
阻まれます。

## ライセンス

Graphica 本体は **MIT License** です。配布物には Qt/PySide6 (LGPL v3) などの
第三者ライブラリが同梱されています。詳細は同梱の `LICENSE` と
`THIRD_PARTY_LICENSES.md` を参照してください。
