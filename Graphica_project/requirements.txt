# 動作確認済みのバージョンに固定している(間接依存は pip に任せる)。

PySide6==6.9.1
matplotlib==3.10.7
numpy==2.3.1
pandas==2.3.1
scipy==1.16.0
openpyxl==3.1.5
xlrd==2.0.2

# --- .exe化 (PyInstaller) を行う場合のみ必要 ---
pyinstaller==6.16.0

# --- テストと静的解析 ---
pytest==9.1.1
ruff==0.16.8
