# トラック3(残りの本体機能追加) 進捗管理

`Graphica_MASTER_SCHEDULE.md` のトラック3(3-1〜3-4、および未分類の残り項目)の進捗を記録する。
このファイルは各項目の「どう実装したか」の詳細ログ。**「今どこまで進んでいて次に何を
やるか」の短い要約は`CURRENT_STATE.md`を先に見ること**(新しいセッションはまず
`CURRENT_STATE.md`を読む運用)。このファイルは項目が完了するたびに追記していく
(上書きしない)。トラック1は`PLUGIN_API_PROGRESS.md`、トラック2は
`GUI_MODERNIZATION_PROGRESS.md`が同じ役割を持つ(対象トラックが異なるだけ)。

## トラック3-1: 解析基盤(優先度高、トラック2完了後すぐ着手)

| ID | 項目 | 状態 | 完了日 | 備考 |
|---|---|---|---|---|
| C-401 | フィット結果の構造化保持 | ✅ 完了 | 2026-08-13 | `core/analysis.py`の`calculate_curve_fit()`の戻り値をタプルからdict化(`popt`/`pcov`/`perr`/`param_names`/`x_fit`/`y_fit`/`r_squared`/`residuals`/`x_data_used`/`y_data_used`)。共分散行列(`pcov`)は従来`_`で捨てていたのを`perr = sqrt(diag(pcov))`として活用可能にした(C-405信頼帯の土台)。`core/dataset.py`の`Dataset`に`fit_result: dict`フィールドを追加(`fit_info`の表示用文字列とは別に、プログラムから再利用しやすい構造化データを保持)。`gui/mixins/dataset_mixin.py`の`_on_fit_curve`/`_on_batch_curve_fit`両方が`_build_fit_result_dict()`経由でJSON/pickle両対応のプレーンなdict(numpy型は全てPython組み込み型に変換済み)を組み立てて`Dataset.fit_result`に格納する。呼び出し側の戻り値アンパックが約20箇所(本体2箇所+テスト)あり、全て新形式に追従済み |
| C-308 | ベースライン補正(ALS/多項式/ラバーバンド/手動点) | ✅ 完了 | 2026-08-13 | `core/analysis.py`に4関数を新設: `calculate_baseline_als`(Eilers & Boelens法、2階差分の疎行列によるTikhonov正則化+非対称重み反復)、`calculate_baseline_polynomial`(Lieber & Mahadevan-Jansenの反復多項式フィット法/ModPoly)、`calculate_baseline_rubberband`(Andrewのmonotone chainによる下側凸包)、`calculate_baseline_manual`(指定アンカー点を線形補間の元データから求め、アンカー間を線形または3次スプラインで結ぶ)。4関数とも戻り値を`(x_sorted, baseline, corrected)`に統一。GUIは新設`BaselineCorrectionDialog`(`gui/dialogs.py`、手法をコンボボックス+`QStackedWidget`でパラメータ欄切替、`BatchExportDialog`と同じ構成)+`dataset_mixin.py`の`_on_baseline_correction_dataset`(既存の`_on_savgol_dataset`と同じ「カレント1件→非破壊で新規データセット追加」パターン、右クリックメニューの`Savitzky-Golayフィルタ...`の隣に追加)。ベースライン曲線自体を別データセットとして追加するオプション付き |
| C-907 | データセットの表示/非表示トグル(目アイコン) | 🟡 一部完了 | 2026-08-13 | ユーザー要望により3-1と同時並行で着手。**表示/非表示トグルのみ実装、検索・絞り込みは未着手**(roadmap #150はそのため`false`のまま)。`core/dataset.py`に`visible: bool`フィールド追加(既定True、後方互換)。`gui/canvas.py`の`MplCanvas.redraw_all()`冒頭で`visible=False`のデータセットを一括フィルタ(画面描画・エクスポート双方の唯一の入口のため、ここ1箇所で両方に効く)。`gui/minimap_widget.py`は`redraw_all`を経由しないため個別にフィルタを追加。UIはデータセットツリーに列を1つ追加(`gui/main_window.py`、`QHeaderView.ResizeMode.Stretch`/`Fixed`で列0=名前・列1=目アイコンの幅を固定)し、実体は`gui/dataset_style_icon.py`の`make_dataset_visibility_icon`/`apply_dataset_visibility_text_style`(非表示時は名前をグレーアウト)。クリック検知・Undo対応は`dataset_mixin.py`の`_on_dataset_tree_item_clicked`が既存の`_push_dataset_property_command`/`SetDatasetPropertiesCommand`パターンに乗せる形で実装(`_on_secondary_y_changed`と同じ構成)。アイコンは実物のTabler Icons SVG(`assets/icons/eye.svg`、MIT、`eye-off.svg`は既存流用)を使用 |

| C-403 | パラメータの初期値・固定・範囲拘束UI | ✅ 完了 | 2026-08-14 | `core/analysis.py`の`calculate_curve_fit()`に`p0_overrides`/`fixed_params`/`bounds`引数を追加。`fixed_params`は自由パラメータのベクトルから完全に除外する方式(scipyに渡す`fit_func`をラップし、固定値を元の位置に挿し込んでから元の関数を呼ぶ)を採用し、フィット後に`popt`/`pcov`をパラメータ数ぶんのフルサイズへ復元(固定パラメータの行/列は0、「最適化されていない=不確かさ不明」を表す)。`bounds`使用時は`curve_fit`が`trf`法に切り替わり`maxfev`ではなく`max_nfev`が必要になる点、および`p0`が境界の厳密に内側でないと例外になる点(境界上/境界外の`p0`はわずかに内側へナッジ)に対応。新設`get_fit_param_names(fit_type, custom_formula)`(実際のフィット計算をせずパラメータ名一覧だけ返す軽量関数、`calculate_curve_fit`内の判定チェーンと意図的に重複させてあり、双方に「新しい組み込みフィットタイプを追加する際は両方更新すること」との注意コメントを配置)を使い、`gui/dialogs.py`の`FitDialog`がフィットタイプ/カスタム数式の変更のたびに`QTableWidget`ベースのパラメータテーブル(初期値・固定チェック・範囲拘束チェック+最小/最大)を再構築する。`FitDialog.get_fit_type()`の戻り値は4要素から7要素のタプルに拡張(`p0_overrides`/`fixed_params`/`bounds`を追加、未カスタマイズ時も空dict)。`Dataset.fit_result`にもこれらをprovenanceとして記録 |
| C-311 | 区間積分(台形則/Simpson則) | ✅ 完了 | 2026-08-14 | `core/analysis.py`に`calculate_interval_integral(x_data, y_data, x_range, method, subtract_baseline)`を新設。`method`は`"trapezoid"`(`numpy.trapezoid`、無ければ`numpy.trapz`にフォールバック)または`"simpson"`(`scipy.integrate.simpson`)。`subtract_baseline=True`で積分範囲の両端(実データを線形補間して求めたY)を結ぶ直線を差し引いてから積分(ALS等の本格的なベースライン補正、既存のC-308とは独立した軽量オプション)。ロードマップの「既存カーソルツールの拡張」という表現は現状のコードと合致しなかったため、`FitDialog`のフィット範囲指定(C-404)と同じ「数値でXの範囲を指定する」UXの`IntervalIntegralDialog`+`_on_savgol_dataset`と同じ「カレント1件」メニューパターンを採用(グラフ上ドラッグ選択は別項目C-909、未実装)。結果はスカラー値のため新規データセットではなく`ResultDialog`で表示(ピーク検出と同じ方針) |
| C-411 | ピーク定量表(位置/高さ/FWHM/面積/重心) | ✅ 完了 | 2026-08-14 | `core/analysis.py`に`calculate_peak_quantification(x_data, y_data, peak_type, settings)`を新設。既存の`calculate_peaks`とピーク/谷の符号反転ロジック(`_peak_detection_signal_and_kwargs`)を共有し、片方だけ直し忘れる過去の実バグ(`test_downward_peak_height_threshold_uses_correct_sign_for_valleys`)を再発させない構成。FWHMは`scipy.signal.peak_widths(rel_height=0.5)`をXへ線形補間。ローカル基線は`rel_height=1.0`で求めた左右の裾野(base)の実測Y値を結ぶ直線(裾野の高さが左右非対称でも自然に扱えるようにするため)。面積は基線差し引き後のYを裾野範囲で台形則積分、重心は基線からの高さ(負はゼロクリップ)で重み付けしたXの加重平均(大きな一定オフセットに乗ったピークで重心が遠いデータ点に引っ張られないようにするため)。谷(下に凸)のarea/centroidは反転ドメイン上でそのまま計算するため、peak_typeによらず常に正の「突出量」として統一的に扱う(X座標は反転の影響を受けないため問題なし)。`_on_find_peaks`を拡張し、`ResultDialog`のテーブルに常にFWHM/面積/重心X列を追加(既存の`calculate_peaks`自体はシグネチャ・戻り値とも無改修) |

**レビュー中に発見・修正したバグ**: C-403のテスト`test_batch_curve_fit_applies_fixed_params_to_all_datasets`が`_on_batch_curve_fit()`終了時の`QMessageBox.information`呼び出しをモックし忘れており(他の同種テストは`_patch_info_capture(monkeypatch)`で塞いでいるが、この1件だけ抜けていた)、ヘッドレス(`QT_QPA_PLATFORM=offscreen`)環境で本物のモーダルダイアログが`.exec()`でイベントループをブロックし続け、テストプロセスがCPU使用率0%のまま完了しない状態になっていた(`ps`ではWindowsネイティブプロセスが見えず気づきにくく、PowerShellの`Get-Process`でCPU時間の増分を計測して初めてハングと判明)。`_patch_info_capture(monkeypatch)`の呼び出しを追加して解消。あわせて、セッション中の別のバックグラウンドAgentが指示に反して素の`pytest -q`(全件・チャンク分割なし)を実行し、2時間以上ハングしたまま残っていたプロセスも発見し停止した(chunked scriptを使わない単発pytestの既知のハング問題、`docs/CURRENT_STATE.md`の「既知の注意点」参照)。

| C-408 | モデルライブラリ拡充(Voigt/pseudo-Voigt/Lorentzian/2成分指数/Boltzmann/Hill) | ✅ 完了 | 2026-08-14 | `core/analysis.py`の`calculate_curve_fit`ディスパッチと`get_fit_param_names`両方に6モデルを追加(`_BUILTIN_FIT_TYPE_SUBSTRINGS`にも登録)。Voigtは`scipy.special.wofz`(Faddeeva関数)を使い、`wofz(0)=1`のガウシアン極限で振幅パラメータ`a`が`a/(sigma*sqrt(2*pi))`のピーク高さになるよう正規化(γ→0でガウシアン、σ→0でローレンツィアンに退化することを確認)。pseudo-Voigtは中心・FWHM(`c`)を共有したガウシアン/ローレンツィアン項の線形結合。Boltzmannシグモイドは既存の`sigmoid_func`と異なり上下漸近値が独立(`a1`,`a2`)。Hill式は`x<0`のみ拒否(`x=0`は有効)。新モデルの文字列("2成分指数関数"⊃"指数関数"等)が既存/他の新規タイプ名の部分文字列と衝突するため、判定チェーン内での順序に注意が必要だった(`★`コメントで明記)。p0推定は半値幅(FWHM)を実データから求める共通ヘルパー`estimate_fwhm()`を新設し、ガウシアン同様のデータ駆動型初期値推定に統一 |
| C-405 | 信頼帯・予測帯の描画 | ✅ 完了 | 2026-08-14 | `core/analysis.py`に`calculate_confidence_band(x_eval, fit_func, popt, pcov, residuals, confidence, band_type)`を新設。非線形回帰の信頼区間は閉形式で求まらないため、線形化(デルタ法): 各評価点でfit_funcをパラメータについて中心差分数値微分してヤコビアンJを求め、Y推定値の分散を`diag(J @ pcov @ J.T)`として伝播(`np.einsum`で対角成分のみ効率的に計算)、t分布の臨界値で区間化。予測帯はこれに残差の平均二乗誤差(観測ノイズ分散)を加算(信頼帯より必ず広くなる)。`calculate_curve_fit()`の戻り値に`fit_func`(ラップ前の元関数)を追加し、フィット直後にその場で帯を計算できるようにした(callableはpickle/JSON化できないため`Dataset.fit_result`には含めず、計算結果の数値配列だけを保持する設計)。`FitDialog`に信頼帯/予測帯選択コンボを追加(`get_fit_type()`の戻り値が8要素タプルに拡張、全呼び出し元・テストヘルパーを追従済み)。フィットデータセットの`df`に`y_lower`/`y_upper`列を追加し、新設`Dataset.fit_band_display`("confidence"/"prediction"/None)と組み合わせて`gui/canvas.py`の`redraw_all()`1箇所でfill_between描画(画面・エクスポート両方に自動的に効く、C-907のvisible列フィルタと同じ設計方針) |
| C-413 | フィット結果の出力(曲線の新規データセット化/表CSV/注釈焼込) | ✅ 完了 | 2026-08-14 | ロードマップの文言のうち「曲線の新規データセット化」「表CSV」はC-401の時点で`_on_fit_curve()`が既に提供済みと判明したため、実際の対応ギャップを精査: (a) フィットをやり直さずに`Dataset.fit_result`だけから表/CSVを再表示する経路が無い、(b) 結果をグラフ上の注釈として焼き込む機能が無い、の2点に絞って実装。新設`_on_export_fit_result()`(右クリックメニュー「フィット結果のエクスポート...」、`fit_result`を持たないデータセットではグレーアウト)が`calculate_curve_fit()`を一切呼ばず`dataset.fit_result`のみから`ResultDialog`を再構成(新設`_format_fit_result_text()`)。注釈焼込は`_burn_fit_result_annotation()`が既存の注釈システム(`gui/mixins/annotation_mixin.py`の`_add_annotation()`、`SetAnnotationsCommand`経由)にそのまま乗せるため、焼き込み後は手動注釈と同じくUndo対応・注釈モードでの移動/編集/削除が可能。アンカー位置はフィット曲線の中央インデックス(線形/指数等の単調な形状でもargmaxのように端に寄らないため)。`_add_annotation()`に任意の`description`引数を追加(Undo履歴の説明文をカスタマイズ可能に、デフォルト引数のため既存呼び出しは無変更) |
| C-305 | 共通X格子へのリサンプリング/補間 | ✅ 完了 | 2026-08-14 | 既存の「データセット間演算」(`_on_dataset_arithmetic`)が2データセット限定・線形補間限定でX格子合わせを内製(`np.interp`)していたのに対し、汎用・再利用可能な独立機能として`core/analysis.py`に`calculate_resample_to_grid(x_data, y_data, target_x, method, extrapolate)`を新設(`_on_dataset_arithmetic`自体は無改修、リグレッションリスクを避けるため)。`method`は"linear"(`np.interp`)/"cubic"(`CubicSpline`)。`extrapolate=False`(既定)では範囲外をNaNにする安全側デフォルト、`True`では"linear"は端点2点の傾きで真の外挿(`np.interp`は既定で範囲外を端の値にクランプするだけなので明示的に外挿処理が必要だった)、"cubic"は`CubicSpline`自身の`extrapolate=True`をそのまま使用。重複X値は最後の値を採用して統合(`CubicSpline`が厳密な単調増加を要求するため)。GUIは新設ダイアログで、ターゲット格子を「他のデータセットのX格子」または「等間隔格子(開始・終了・点数)」から選択可能 |

| C-406 | 残差プロット(専用ドックパネル) | ✅ 完了 | 2026-08-14 | ロードマップ原文の「上下連動2段パネル」を、メインプロットのサブプロットセル内に実際に2段のAxesとして埋め込む形(sharex等)ではなく、独立したドックパネル(`gui/residual_panel.py`の`ResidualPanel`)として実装した(ユーザーに実装イメージを可視化して確認の上で決定)。理由: `gui/canvas.py`のサブプロットグリッド/自由配置/第2Y軸/ミニマップ連動といった既存機構と絡み合う実装(埋め込み型)はリグレッションリスクが高く、代わりに既存の「メインキャンバスとは別の、選択状態に連動する小さな独立パネル」というパターン(ミニマップ`gui/minimap_widget.py`、エクスポートプレビュー`gui/export_preview_panel.py`)を踏襲することで、`gui/canvas.py`本体を一切変更せずに実装できた。選択中のデータセットが`fit_result`(項目C-401で永続化済み)を持っていれば`residual_x`/`residuals`をそのまま散布図表示(再計算しない)、持っていなければプレースホルダ文言を表示。`gui/mixins/dataset_mixin.py`の`_update_ui_state()`(データセット選択変更のたびに呼ばれる既存の一元的なUI更新関数)に1行追加する形でパネルの`refresh(dataset)`を呼ぶ。エクスポートプレビューと同じ理由(常時描画の負荷回避)で既定非表示、View メニューから`QDockWidget.toggleViewAction()`でトグル。「連動」の意味は、軸範囲のリアルタイム相互同期(パン/ズーム)ではなく、選択中のフィットデータセットへの追従として解釈した(実装時の判断としてドキュメント化) |

## トラック3-1 進捗まとめ

**10項目全て完了(2026-08-14)。トラック3-1(解析基盤)完了。PR経由でmasterへマージ・push済み(マージコミット`ce4a13a`)。**

## トラック3-2: 性能・体験(`feature/performance-ux`ブランチ、git worktree `PlotterApp-performance-ux`で並行開発)

ユーザー指示により、大型2項目(C-003 再描画スコープ分離、C-004 ワーカースレッド基盤)は
既存アーキテクチャへの影響が大きいため別途相談してから着手することとし、まず残り6項目
(C-1001, C-902, C-908, C-909, C-601, C-602)を先に完了させる方針。

| ID | 項目 | 状態 | 完了日 | 備考 |
|---|---|---|---|---|
| C-1001 | 表示用ダウンサンプリング(LTTB) | ✅ 完了 | 2026-08-14 | `core/analysis.py`に`calculate_lttb_downsample(x_data, y_data, n_out)`を新設(Largest-Triangle-Three-Buckets法、先頭・末尾は常に残す)。既存点群を間引いた**新しい点を合成するのではなく、元の配列に対するインデックスを返す**設計とし(表示用の別データを持たせるとカーソル/範囲選択等の既存機能全てが二重管理になるため)、`gui/canvas.py`の`MplCanvas`に`downsample_index_map`(dataset_id→インデックス配列)を追加してこれを保持。`_draw_data()`内、waterfallオフセット適用後・平滑化/plot_type分岐前に注入し、`plot_type`がLine/Scatter/Line+Scatterかつ非カテゴリ軸かつ点数が閾値(`LTTB_DOWNSAMPLE_THRESHOLD=20000`)超かつX昇順(`np.all(np.diff(x)>=0)`)の場合のみ適用(降順/非単調データ・カテゴリ軸ではLTTBのバケット分割ロジックが前提とする単調性が崩れるため意図的にスキップ)。誤差棒(`x_err_data`/`y_err_data`)も同じインデックスでサブセットしないと元データとの長さ不一致でクラッシュする実バグを発見・修正。カーソルツール(`gui/mixins/cursor_mixin.py`の`_on_pick`)がクリックしたのは間引き後の点のインデックスのため、`downsample_index_map`経由で元の`visible_df`のインデックスへ変換してから使う処理を追加(変換を忘れると間引き時にクリック→誤ったデータ点を指す静かなバグになるため重要) |
| C-902 | 一括スタイル適用(複数データセットへのスタイルコピペ) | ✅ 完了(既存実装確認) | 2026-08-14 | 実装前に既存コードを調査した結果、データセットツリーの複数選択+右クリック「スタイルを貼り付け」が既に単一Undoマクロで複数データセットへ一括適用できる状態であることが判明。ロードマップ記載の要件を重複実装せず、ユーザーに確認の上で完了扱いとした(新規コード変更なし) |
| C-908 | マウス操作拡充(スクロールズーム/中ボタンパン) | ✅ 完了 | 2026-08-14 | `gui/mixins/cursor_mixin.py`に`_on_scroll_zoom(event)`(`event.inaxes`のデータ座標系でのカーソル位置を軸範囲に対する比率(`relx`/`rely`)として保持し、ズーム後も同じ比率になるよう新しい`xlim`/`ylim`を計算する「カーソル位置を中心にズーム」方式)と`_on_middle_button_press_pan`/`_on_middle_button_motion_pan`/`_on_middle_button_release_pan`(移動量はプレス時点を基準に都度計算し、モーション毎の差分累積にしない方式でドリフトを回避)を追加。`gui/main_window.py`の`__init__`で`canvas.mpl_connect('scroll_event', ...)`等を1回だけ接続(常時有効、カーソル/注釈/レイアウト編集/範囲選択モードのような排他トグルではない)。カーソルモード切替(`_toggle_cursor_mode`)は範囲選択モードとの排他制御も同時に追加(後述C-909参照) |
| C-909 | グラフ上での範囲選択(ドラッグでマスク) | ✅ 完了 | 2026-08-14 | 新設`gui/mixins/range_select_mixin.py`の`RangeSelectMixin`。実装当初、既存のミニマップ(`gui/minimap_widget.py`)で使われている`matplotlib.widgets.SpanSelector`の流用を検討したが、SpanSelectorは特定のAxesインスタンスに紐付く一方、メインキャンバスは`redraw_all()`のたびに`fig.clf()`で全Axesを作り直す設計(既知の制約、`gui/canvas.py`のdocstring参照)のため毎回作り直しが必要になり不適合と判断。代わりにカーソル/注釈/レイアウト編集モードと同じ「キャンバスレベルのイベント接続を常設し、ハンドラ内で`event.inaxes`を毎回読む」パターンを踏襲(`_on_range_select_press/motion/release`)。ドラッグ確定時、`_apply_range_mask`がドラッグ先の軸に実際に描画されているデータセットのみを対象に判定(`dataset.subplot_target`/`use_secondary_y`と`self.all_axes`/`self.all_secondary_axes`の対応を照合)し、範囲内の行を`SetMaskedRowsCommand`でUndo対応のマスク(非破壊)にする。カーソル/注釈/レイアウト編集の既存3モードとも双方向排他化(いずれかON時に範囲選択モードをONにしようとすると自動的に元のモードをOFFにし、逆も同様) |
| C-601 | 軸共有(sharex/sharey) | ✅ 完了 | 2026-08-14 | `models/project.py`の`ProjectModel`に`share_x_axis`/`share_y_axis`(既定False)を追加し、pickle/JSON双方の保存・読込4メソッドで永続化(旧形式ファイルにキーが無い場合はFalseにフォールバック)。`gui/canvas.py`の`redraw_all()`にグリッドレイアウト時のみ意味を持つ`share_x_axis`/`share_y_axis`引数を追加し、サブプロット生成ループで全サブプロットを`self.all_axes[0]`と`sharex`/`sharey`で束ねる(matplotlibの`plt.subplots(sharex=True)`と同じ「グリッド全体で共通」方式。「同じ行/列のみ共有」ではなく、よりシンプルな全体共有を採用)。内側の目盛りラベル(最下行以外のX軸ラベル・最左列以外のY軸ラベル)は共有時は冗長なため、目盛り自体は残したまま`ax.tick_params(labelbottom=False)`/`labelleft=False`でラベル文字のみ非表示にする。UIはレイアウト設定フォームに`share_x_checkbox`/`share_y_checkbox`(`QCheckBox`)を追加し、`gui/mixins/settings_mixin.py`の新設`_on_share_axis_changed()`(チェック状態→`self.project.share_x_axis`/`share_y_axis`→`_update_plot()`)で反映。自由配置レイアウト時はrows/colsスピンボックスと同様に意味を持たないため、`_on_toggle_free_layout`(`gui/mixins/layout_edit_mixin.py`)で自由配置ON中はチェックボックス自体を無効化。プロジェクト読込(`_load_project_from_path`)でも保存済みの値をチェックボックスへ復元し、自由配置状態に応じた有効/無効も同時に適用 |
| C-602 | 単位変換の第2X軸(nm↔eV↔cm⁻¹↔Hz) | ✅ 完了 | 2026-08-14 | 新設`core/unit_conversion.py`の`convert_x_axis_unit(value, from_unit, to_unit)`。nm以外の3単位(eV/cm⁻¹/Hz)はいずれも波長に反比例する量(`value = 定数 / wavelength_nm`)であるため、任意の単位ペア間の変換を「波長(nm)を経由する」形で統一的に実装(hc定数≒1239.842 eV·nm、1e7 cm⁻¹·nm、光速×1e9 Hz·nmの3定数のみで足りる)。UIはX軸タブに`x_secondary_axis_source_unit_combo`(X軸データの単位)/`x_secondary_axis_target_unit_combo`(第2X軸に表示する単位)の2つの`QComboBox`を追加し、`all_plot_settings`(サブプロットごとの既存の設定辞書、ProjectModel側の新規フィールド追加は不要)に`x_secondary_axis_source_unit`/`x_secondary_axis_target_unit`として保存。両方が「なし」以外かつ異なる場合のみ、`gui/canvas.py`の`_apply_appearance()`末尾で`ax.secondary_xaxis('top', functions=(forward, inverse))`により変換済みの第2X軸を追加(Y2軸と同じ外観設定=フォント/色/目盛り方向を流用)。日付軸/カテゴリ軸は対象外。**レビューで発見・対処した実バグ**: X軸の表示範囲が0を含む(実データでautoscaleが0始まりになるのは珍しくない)状態でnm↔eV等の変換を有効にすると、変換後の値がinf/nanになり`ax.secondary_xaxis()`がグラフ全体の再描画を巻き込んで例外(`ValueError: Axis limits cannot be NaN or Inf`)を投げていたため、`_apply_appearance()`側で変換後のxlimが有限であることを事前チェックし、該当する場合は第2X軸自体を追加しない(メインの描画には影響させない)形で回避。**もう1件**: 新設の2コンボボックスの初期ラベル文言(「X軸データの単位」「第2X軸に表示する単位」)がQFormLayoutの共有ラベル列幅を圧迫し、既存の回帰テスト`test_properties_dock_has_no_horizontal_scrollbar`(項目102、CONTROL_DOCK_WIDTHのマージン確保)を壊す実害が発生。ラベルを「X軸単位」「第2X軸単位」に短縮し詳細説明はツールチップへ移すことで解消(CLAUDE.mdに既に明記されている「formLayout系は全行でラベル列幅を共有する」という既知の注意点の再確認) |

**レビュー中に発見・修正したバグ(C-909テストのハング)**: 新設テストの`_add_dataset`ヘルパーが`window.project.datasets.append(ds); window._update_plot()`のように内部状態へ直接データセットを追加していたため、`_add_dataset_list_item()`を経由せず`QTreeWidgetItem`が作られない状態になっていた。その結果`_get_current_dataset()`が常に`None`を返し、`_apply_range_mask`の`if dataset is None:`分岐が未モック`QMessageBox.information`を呼び出してヘッドレス環境でハング(C-403レビュー時に発見した同種のバグパターンと同じ、`docs/CURRENT_STATE.md`参照)。単発の`python -c "..."`スクリプトで`current dataset: None`と出力させて原因特定し、テストヘルパーを実際の`window._add_dataset(ds, select=select)`経由に修正して解消。

| C-003 | 再描画スコープ分離(フェーズ1-3) | ✅ フェーズ1-3完了 | 2026-08-16 | 計画モード(3体のExploreエージェント+1体のPlanエージェントによる事前調査)を経てから着手。`gui/canvas.py`に`update_single_axis(axis_index, datasets, settings, rows, cols, share_x_axis, share_y_axis, panel_labels_enabled)`を新設し、`ax.cla()`(`fig.clf()`ではない)で対象の1つのAxesだけをクリアして`_draw_data`→`_apply_appearance`→`_draw_annotations`(→`_draw_panel_label`)を再実行、他のAxes・Figure自体には触れない。`twinx()`で作られた副軸は`ax.cla()`では消えない別のAxesオブジェクトのため明示的に`.remove()`してから作り直す処理、`ax.cla()`が`tick_params`をリセットすることでC-601の軸共有時の内側目盛りラベル非表示が復活してしまう問題への対処(`_apply_shared_axis_tick_visibility()`という共通ヘルパーに切り出し`redraw_all()`・`update_single_axis()`の両方から呼ぶ)が実装上の勘所。**レビューで発見・修正した実バグ(フェーズ1時点)**: 既存テスト`test_dataset_tree_item_clicked_triggers_replot`(データセット表示/非表示トグル、項目C-907)が`window._update_plot`をモックして呼び出しを検証していたが、visible属性は非構造的プロパティのため`update_single_axis()`経由に切り替わり、モック対象が古いまま失われていた(実装のバグではなく、最適化が正しく効いた結果としてテストの想定が古くなっていたケース)。モック対象を`canvas.update_single_axis`に更新して解消。**フェーズ2(パネルラベル/ダークモード切替の軽量化)**: `update_single_axis()`本体を`self.draw_idle()`を呼ぶ直前まで(`_redraw_single_axis_no_draw()`)と`draw_idle()`自体に分離した上で、新設`update_all_axes_appearance_and_data(datasets, rows, cols, all_plot_settings, layout_mode, panel_labels_enabled, share_x_axis, share_y_axis)`が既存Axes全件ぶん`_redraw_single_axis_no_draw()`をループし(Axesごとの`draw_idle()`呼び出しは行わない)、最後に`redraw_all()`と同じFigureレベルの処理(facecolor設定・グリッドレイアウト時のみの`tight_layout()`・実`draw()`・`is_secondary_visible_global`の再計算)をループの外側で1回だけ実行する。`gui/main_window.py`の`_update_plot(light=False)`に`light`引数を追加し、`light=True`時のみ`canvas.update_all_axes_appearance_and_data()`を呼ぶよう分岐。呼び出し側3箇所(`_on_toggle_panel_labels`、`gui/mixins/ui_setup_mixin.py`の`_on_toggle_dark_mode`、`_sync_dark_mode_to_sibling_tabs`)を`light=True`化。**フェーズ3a(subplot_target/use_secondary_yの軽量化)**: 従来これら2プロパティは`_STRUCTURAL_DATASET_PROPERTIES`として一律フルの`_update_plot()`に振り分けていたが、`use_secondary_y`は現在の`subplot_target`軸1つだけで完結し(`update_single_axis()`が既にtwinx()の作成/削除を正しく扱う)、`subplot_target`自体の変更も「旧軸から消える・新軸に現れる」という2つのAxesだけで完結する(Axes枚数・GridSpec配置自体は変わらない)ため、`update_single_axis()`を対象Axes数ぶん呼ぶ軽量パスに置き換えた。`_push_dataset_property_command()`が`SetDatasetPropertiesCommand.on_applied`へ`old_values`/`new_values`両方を渡すよう変更(`on_applied`はredo/undoどちらの後でも同じコールバックが呼ばれ方向を教えてくれないため、`_refresh_after_dataset_property_change()`は現在値+old_values+new_valuesの`subplot_target`候補を集合として重複排除することで、redo/undoどちらの向きでも新旧両方のAxesを正しく更新する)。`_STRUCTURAL_DATASET_PROPERTIES`定数は不要になったため削除。**フェーズ3b(自由配置レイアウトのサブプロット追加/削除の軽量化)**: 「+ プロット追加」は常に末尾に空のAxesを1つ追加するだけ、「- プロット削除」は常に末尾のAxesのみを削除する仕様(`gui/mixins/layout_edit_mixin.py`の`_on_add_free_subplot`/`_on_remove_free_subplot`)であることを利用し、途中のAxesのインデックス振り直しが一切不要な点に着目。新設`canvas.add_free_axis(datasets, settings, panel_labels_enabled)`(`fig.add_axes()`で新規Axesを1つ追加し`_draw_data`/`_apply_appearance`/`_draw_annotations`を実行するのみ、他のAxesには一切触れない)と`canvas.remove_last_free_axis(datasets)`(末尾のAxes・副軸を`.remove()`し、`all_axes`/`all_secondary_axes`/`axis_is_date_x`/`axis_is_category_x`から`.pop()`、削除軸に紐づく`_annotation_artists`/`_highlight_artists`/`downsample_index_map`を後片付け)を新設。削除時は「削除された番号のサブプロットに割り当てられたデータセットを新しい末尾へ付け替える」既存の仕様(項目C-907と同種のバグ修正で導入済み)があるため、`remove_last_free_axis()`(削除Axesの後片付け)に続けて`update_single_axis()`(付け替え先の新しい末尾Axesへのデータ反映)を呼ぶ2手順で構成。**技術的な確認事項**: `_highlight_artists`/`downsample_index_map`はデータセットIDをキーにしており、移動元Axesの`cla()`/`.remove()`で実体のArtistは既に破棄されるが、辞書エントリ自体は移動先Axesを更新する2手順目の`update_single_axis()`(呼び出し時点でdataset.subplot_targetは既に新値)のクリーンアップループが正しく回収することを確認済み(`set_highlighted_points()`側も`old_artist.remove()`を`try/except (ValueError, NotImplementedError)`で守っているため、仮に一時的にstaleな参照が残っても例外にならない設計)。グリッドレイアウトの行数/列数変更のみ、`sharex`/`sharey`の再結線が絡み頻度も低い操作のため、リスクの割にメリットが小さいと判断し意図的にフルの`_update_plot()`のまま残した(`docs/roadmap.html`の#61タイトル注記にその旨を明記) |
| C-004 | ワーカースレッド基盤 `TaskRunner`(フェーズ1-5) | ✅ フェーズ1-5完了 | 2026-08-16 | 新設`gui/task_runner.py`の`TaskRunner`(`QThread`サブクラス、`gui/workers.py`の`DataLoadWorker`と同じ設計・エラー規約=`str(例外)`のみをシグナルで運ぶ、を踏襲しつつ特定処理専用ではなく任意の関数を注入できるよう一般化)。**フェーズ1(単発カーブフィット)**: `core/analysis.py`に薄いラッパー`fit_curve_task()`を新設し`_on_fit_curve()`をバックグラウンドスレッド化(scipyの`curve_fit`自体は中断不能なため、目的はキャンセル機能ではなくスレッド起動→シグナル配送→GUIスレッドでの適用→`closeEvent`での中断待機という配線の検証)。**フェーズ2(一括カーブフィット)**: 従来はループ内で`_add_dataset()`(内部で毎回`_update_plot()`を呼ぶ)を都度呼んでおり、N件のフィットでN回のフル再描画が起きていた実バグ的挙動を、`_batch_fit_worker()`で全件をバックグラウンドスレッド上で計算し成功結果をまとめて追加してから`_update_plot()`を1回だけ呼ぶ形に変更して解消(この修正自体はC-003の有無に関係なく単独で価値がある)。初のUI進捗表示として`QProgressDialog`(Cancelボタン→`requestInterruption()`)を追加。**フェーズ3(バッチエクスポート、縮小版)**: `_batch_export_subplots`/`_batch_export_project_files`は項目ごとに新規`MplCanvas`(`QWidget`のサブクラス)を生成しており、QWidgetをGUIスレッド以外で構築するのはQt的に安全でないため、`TaskRunner`(別スレッド)には乗せずGUIスレッド上で`QProgressDialog`+項目間の`processEvents()`によるキャンセル可能な進捗表示のみを実装。**フェーズ4(DataLoadWorkerの統合)**: 当初「フェーズ1-3ではDataLoadWorker自体はTaskRunnerへ移行しない」としていた判断を覆し、`gui/workers.py`の薄いラッパー`load_data_file_task(file_path, report_progress=None, is_cancelled=None)`(`read_data_file()`+列数バリデーションをまとめたもの、`read_data_file()`自体は中断不能な単一のブロッキング呼び出しのため`report_progress`/`is_cancelled`は受け取るだけで使わない)を新設し、`DataLoadWorker`クラス自体を削除。`gui/main_window.py`の`load_data()`を`TaskRunner(load_data_file_task, file_path, parent=self)`経由に置き換え、`self._data_load_worker`→`self._data_load_task_runner`に改名した上で、`closeEvent`のクリーンアップブロックを`_fit_task_runner`/`_batch_fit_task_runner`と同型(シグナル切断→`requestInterruption()`→`wait()`→`deleteLater()`)に統一(以前は3つのTaskRunner用ブロックがそれぞれ微妙に異なる書き方だったのが、この統一で完全に揃った)。**フェーズ5(バッチエクスポートの実スレッド化、2段階)**: フェーズ3で見送っていた「QWidgetのサブクラスであるMplCanvasをGUIスレッド以外で構築するのは危険」という制約を、`gui/canvas.py`の構造リファクタで解消してから実スレッド化した。**5a**: `MplCanvas`の描画ロジック全体(`redraw_all`/`_draw_data`/`_apply_appearance`等、Qtに依存するのは`__init__`の2行だけだった)を`_CanvasDrawingMixin`(`_init_drawing_state()`+各描画メソッド、`self.fig`とプレーンなPython属性のみ操作)へ切り出し、`MplCanvas(FigureCanvasQTAgg, _CanvasDrawingMixin)`と新設`_HeadlessRenderCanvas(FigureCanvasAgg, _CanvasDrawingMixin)`の両方から共有できるようにした(`gui/mathtext_preview.py`が既に`Figure()`+`FigureCanvasAgg(fig)`をQt非依存で使っている前例があり、`matplotlib==3.10.7`で実際に動作検証済み)。バッチエクスポートの一時キャンバスを`MplCanvas`から`_HeadlessRenderCanvas`に差し替え(この時点ではまだGUIスレッド上で同期実行のまま、`test_canvas.py`/`test_export_mixin.py`とも無改修分で全件成功を確認し「描画結果が変わらないこと」を検証)。**5b**: `_batch_export_subplots`/`_batch_export_project_files`のシグネチャを`progress_dialog`引数から`report_progress=None, is_cancelled=None`(`_batch_fit_worker`と同じ規約)に変更し、`_on_batch_export()`を`_on_batch_curve_fit`と同じ`TaskRunner`配線パターン(`QProgressDialog`生成→`runner.progress`→`setValue`、`progress_dialog.canceled`→`runner.requestInterruption`、`succeeded`/`failed`→新設`_on_batch_export_succeeded`/`_on_batch_export_failed`)に変更してバックグラウンドスレッド化。`self._batch_export_task_runner`を`__init__`/`closeEvent`に追加(既存3種のTaskRunnerと同型のクリーンアップ)。`_save_figure_with_options`が使う`mpl.rc_context()`がプロセスグローバルなrcParamsを書き換えるため、複数`TaskRunner`を同時に走らせる設計にはしない(既存の逐次forループのままバックグラウンドスレッドを1つだけ使う)という制約を踏襲。**レビューで発見・修正した実バグ(最重要、フェーズ1-3時点)**: 当初`TaskRunner.run()`は「`isInterruptionRequested()`中は`succeeded`シグナルを抑制する」というポリシーを持たせていたが、これはフェーズ1(中断不能な単発フィット、キャンセルUIなし)では無害だった一方、フェーズ2の一括フィット(`is_cancelled()`をアイテム間でチェックし完了済み分の部分結果を正常returnする、意図的にキャンセル対応した設計)では、正常に返ってきた部分結果ごと握りつぶされ`succeeded`/`failed`のどちらも発火せず、呼び出し元(`_pump_events_until_batch_fit_task_done`)が永遠に完了を待ち続けるテストのハングとして発覚。原因を切り分けた上で、キャンセル時に何を結果として扱うかは全面的にワークファンクション側の責任とする設計に変更(`TaskRunner`は`fn`が例外を投げずに返した値を、`isInterruptionRequested()`の状態に関わらず常にそのまま`succeeded`で中継するだけにした)。バッチフィットのキャンセルテストはタイミング依存(4件×0.2秒の遅延を挟み、t=0.35秒でキャンセル要求を出す設計、3回連続実行して再現性を確認済み) |

## トラック3-2 進捗まとめ

**8項目全て完了(2026-08-15、うちC-003/C-004はスコープを絞ったフェーズのみ)。**
C-601/C-602完了後、C-003フェーズ1完了後、C-004フェーズ2/3完了後のそれぞれで
フルスイートを実行し、いずれも実際のテスト失敗ゼロを確認済み。C-003・C-004とも
「フェーズ1のみ」「フェーズ1-3のみ」という、計画モードで事前に立てユーザー承認を
得た縮小スコープでの完了であり、`docs/roadmap.html`の#61/#62は説明文の全範囲を
実装したわけではないため`true`にせず、タイトル注記で範囲を明示したまま`false`を
維持している(詳細は上記の各行、および`docs/CURRENT_STATE.md`参照)。

**追記(2026-08-16)**: ユーザーからの追加指示により、C-003/C-004で意図的に据え置いていた
3項目(構造的トリガーの軽量化=C-003フェーズ2、DataLoadWorkerのTaskRunner統合=C-004
フェーズ4、バッチエクスポートの実スレッド化=C-004フェーズ5)全てに着手・完了した。
フェーズ4は`feature/worker-migration`ブランチで完了・フルスイート(チャンク分割実行、
113件/113件成功)確認済み、masterへマージ済み。C-003フェーズ2・C-004フェーズ5a/5bは
`feature/canvas-light-redraw`ブランチで完了(詳細は上記C-003/C-004の各行を参照)。
フルスイート実行中、`test_export_preview_panel.py`を含む40件チャンクで全テスト
成功後にプロセスがSIGSEGVでクラッシュする事象が発生したが、これは
`docs/CURRENT_STATE.md`の「既知の注意点」に既に記録済みの既知の問題
(pytest自体は'N passed'を報告済みでテスト内容自体は正しい、インタプリタ
終了処理でのクラッシュ)。同一チャンクを無改修のmasterでも独立に再現し、
今回の変更とは無関係であることを確認済み。

**追記2(2026-08-16、続き)**: ユーザーから、C-003/C-004の残課題のうち
「trueにするなら何が残っているか」を問われたのを受けて技術的な棚卸しを実施。
C-004(#62)はバックログの元の目標(`docs/Graphica_CORE_BACKLOG.md`記載の
「インポート/バッチ/フィットのUIブロック解消」)をフェーズ1-5で既に全て
カバーしていたため、追加実装なしでtrue化。C-003(#61)は残っていた3つの
構造的トリガー(グリッド行数/列数変更・自由配置のサブプロット追加削除・
`subplot_target`/`use_secondary_y`変更)のうち、後者2つが技術的に軽量化
可能と判断して`feature/canvas-scope-phase3`ブランチで着手・完了(フェーズ
3a/3b、詳細は上記C-003の行を参照)。グリッド行数/列数変更のみ、リスクの
割にメリットが小さいため意図的に対象外のまま残し、true化した。

3体のExploreエージェントによる事前調査(フィット基盤・Datasetのフィールド
構成/永続化パターン・既存の書式テンプレート機能・`_apply_appearance`の
全プロパティ一覧)を経てから着手。C-1101/C-1102は`feature/provenance-tracking`
ブランチで先行完了させ(C-409/C-410がフィット結果へのprovenance付与で
依存するため)、C-1103/C-806は独立して`feature/export-and-templates`
ブランチで並行、C-409/C-410はprovenance基盤マージ後に`feature/multi-peak-fitting`
ブランチで着手する計画(計画モードでユーザー承認済み、詳細は当時のplanファイル参照)。

| ID | 項目 | 状態 | 完了日 | 備考 |
|---|---|---|---|---|
| C-1101 | provenance(処理履歴・親子関係)の記録とツリー表示 | ✅ 完了 | 2026-08-16 | `core/dataset.py`の`Dataset`に`provenance: dict`フィールドを新設(`default=None`、`fields(self)`ベースの`to_dict`/`from_dict`/`__setstate__`が自動的に拾うため`models/project.py`側の変更は不要)。キー: `operation`(str)・`params`(dict、素のPython型のみ)・`source_dataset_ids`(list[str])・`source_dataset_names`(list[str]、親削除後も表示名を残すため)・`timestamp`(ISO8601)。自身を生成した直近1回分の操作のみを保持し、全履歴は`source_dataset_ids`を辿って`project.datasets`から都度再構築する設計(子孫側に祖先の履歴を重複保持させないため)。`gui/mixins/dataset_mixin.py`に共通ヘルパー`_build_provenance(operation, params, source_datasets)`を新設し、派生データセットを生成する7箇所(`_on_dataset_arithmetic`・`_on_normalize_dataset`・`_on_savgol_dataset`・`_on_baseline_correction_dataset`・`_on_resample_dataset`・`_on_fit_curve_succeeded`・`_batch_fit_worker`)全てに適用。フィット系は既存の`fit_result`(C-401、既に素のPython型のみで構成済み)をそのまま`provenance['params']`として再利用し、重複データ構造を避けた。`SetDatasetPropertiesCommand.on_applied`はredo/undoどちらの後でも同じコールバックが呼ばれる(方向を教えてくれない)ため、`_push_dataset_property_command`が`old_values`/`new_values`の両方をコールバックへ渡すよう変更。新規UI: `gui/provenance_panel.py`の`ProvenancePanel`(`gui/residual_panel.py`と同じ「メインキャンバスとは別の、選択状態に連動する常設ドックパネル」パターン)。ツリー構造は「選択中データセット→操作内容ノード(`core/methods_text.py`の`describe_operation()`で日本語化)→元データセットノード」を1世代の単位とし、元データセット自身もprovenanceを持てば再帰的に祖先まで辿る。親が既に削除済み(`project.datasets`に見つからない)場合は「(削除済み)」と表示して再帰を打ち切る安全策、循環参照(理論上発生しないはずだが壊れた/手編集ファイルへの保険)もvisitedセットで検知して打ち切る |
| C-1102 | 「方法」文の自動生成 | ✅ 完了 | 2026-08-16 | 新設`core/methods_text.py`の`generate_methods_text(dataset, project)`。provenanceチェーンを祖先から順にたどり(`source_dataset_ids`が複数ある場合はデータセット間演算等の合成処理のため単一の直線的な文章では表現しきれず、そこで祖先探索を打ち切って親データセット名を列挙する形に切り替える)、`describe_operation()`(C-1101と共有、operation文字列→日本語ラベルの変換をここに一本化)で各ステップを日本語化して連結する。UIは右クリックメニュー「「方法」文をコピー...」(`_on_copy_methods_text`、`export_fit_action`と同じ「常時メニューに出すが対象外時はグレーアウト」パターン、provenance無しの元データセットでは無効)でクリップボードへコピー |
| C-1103 | Pythonスクリプトとしてエクスポート | ✅ 完了 | 2026-08-16 | 新設`core/script_export.py`の`generate_python_script(project)`。GUI(`gui/canvas.py`)には一切依存しない純粋関数群とし、`project.datasets`/`project.all_plot_settings`だけを読んでPythonソースコードの文字列を組み立てる(`import numpy`/`import matplotlib.pyplot`のみで実行できるスタンドアロンスクリプト、データは`np.array([...])`としてスクリプト内に直接埋め込み、外部ファイル依存を排除)。numpy>=2.0で`repr(np.float64(...))`が`"np.float64(1.5)"`のような冗長な表記になる問題を`_to_native()`(`.item()`でPython組み込み型に変換)で回避、NaNは`repr()`すると未定義の識別子`nan`になり構文エラーになるため`float('nan')`という有効なPython式に明示変換。対応するplot_typeはLine/Scatter/Line+Scatter/Area/Barの組み込み5種のみで、プラグイン提供の`plot_type`(`register_plot_type`)はスクリプト側にプラグインを持ち出せないため、コメント付きでLineとして代替出力する。グリッド/自由配置レイアウト・第2Y軸(twinx)・非表示データセットの除外にも対応。**スコープの意図的な簡略化**(生成スクリプトの先頭コメントにも明記): グラデーション・ウォーターフォール・エラーバー・注釈・パネルラベル・第2X軸の単位変換・グリッド線の詳細設定・日付軸/カテゴリ軸の専用フォーマットは非対応(`_apply_appearance`が持つ240行超のスタイル項目のうち主要なもの=タイトル/軸ラベル/軸範囲/対数軸/凡例表示/グリッド表示のみを再現)。UIは「Pythonスクリプトとしてエクスポート...」メニュー(`gui/mixins/export_mixin.py`の`_on_export_python_script`、既存のバッチエクスポート等と同じ配置)。**テストの要点**: 生成したスクリプトを実際に`exec()`して構文/実行時エラーが無いことを確認(文字列アサーションだけでは検知できない不具合を捕まえるため、これが最も重要な検証観点だった) |
| C-806 | フィギュアテンプレートの独立ファイル化・共有・後適用 | ✅ 完了 | 2026-08-16 | 既存の`_on_save_plot_template`/`_on_load_plot_template`(`gui/mixins/project_io_mixin.py`)を拡張。以前は「現在アクティブな1サブプロットの軸設定のみ」を`.json`へ保存/復元していたが、`format_version`(新形式の目印を兼ねる)・全サブプロット分の外観設定(`subplot_styles`)・全データセットのスタイル(`dataset_styles`、既存の「スタイルをコピー」機能=`_on_copy_dataset_style`が使う`STYLE_ATTRS`と同じキー一覧を再利用し、表記のズレを防いだ)を保存する新形式に拡張。拡張子は`.graphica-style`(中身はJSONのまま)。読込時は現在のサブプロット数/データセット数に応じて保存時の並び順から先頭サイクリックに適用するため、保存時と数が異なっていても破綻しない。**注釈・凡例の並び順・自由配置の位置(`TEMPLATE_EXCLUDED_AXIS_SETTING_KEYS`)は保存/適用の対象から除外**(サブプロットの「内容」寄りで、別のデータセット/プロジェクトへ持ち込む「見た目のスタイル」としては不適切なため、適用後も既存データセット側の値をそのまま保持する)。旧形式(`plot_settings`キーのみ、`format_version`無し)のテンプレートファイルは引き続き「アクティブな1サブプロットのみに適用」という既存の互換動作のまま読み込める(後方互換)。Undo/Redoには対応しない(旧形式のテンプレート適用も同様に非対応だった既存の挙動を踏襲) |

**トラック3-3進捗**: C-1101/C-1102/C-1103/C-806の4項目全て完了。C-409/C-410
(多峰分離+クリック初期値配置UI)はprovenance基盤マージ後に`feature/multi-peak-fitting`
ブランチで着手予定(未着手)。
