# 変更履歴

## v1.3.4 (2026-08-27)

実機(Mac)での利用中に報告された複数の不具合への対応と、目盛りの表示に関する新機能を追加したパッチリリースです。

### 新機能

- 目盛りの数値を小数点以下何桁まで表示するかを、X軸/Y軸それぞれ独立して指定できるようになりました。

### バグ修正

- 自動スケールを解除した直後、最小値/最大値スピンボックスに古い初期値が残ったままになっており、変更する順序によっては軸の範囲が反映されない不具合を修正しました。
- Windows環境で「Arial Narrow」のような幅違いフォント(Narrow/Condensed系)を選択してもグラフに反映されない不具合を修正しました。グラフの描画エンジン(matplotlib)に認識されないフォントを選んだ場合は、その場で警告を表示するようにしました。
- エクスポートプレビューで背景透過を有効にした状態でクリップボードへコピーすると、macOSでは真っ黒な画像として貼り付けられる不具合を修正しました。
- ドック内のQGroupBox(「グラフ全体レイアウト」「編集対象のプロット」等)のタイトル文字が微妙に見切れる表示不具合を修正しました(Windows/macOS共通)。
- グラフの描画中に発生したエラーが、コンソールを持たないexe/appビルドでは完全に握りつぶされ、原因究明の手がかりが残らない問題への対策を行いました。

---

## v1.3.3 (2026-08-19)

v1.3.2で追加した「目盛/目盛の数値の表示切替」機能への追加フィードバックに対応したパッチリリースです。

### バグ修正

- 目盛(目盛線本体)・目盛の数値の表示/非表示が、一度非表示にすると再表示に戻しても反映されない不具合を修正しました。

### 改善

- 目盛・目盛の数値の表示切替を、X軸/Y軸一括ではなくそれぞれ独立して設定できるようにしました。
- 上記の設定項目を、ラベル/書式タブではなくX軸タブ・Y軸タブのそれぞれに移動しました。

---

## v1.3.2 (2026-08-19)

実機での利用中に報告された不具合・改善要望をまとめて対応したパッチリリースです。

### 新機能

- プロジェクトの保存を「上書き保存(Ctrl+S)」と「名前を付けて保存(Ctrl+Shift+S)」に分離しました。以前は保存操作のたびに毎回保存先ダイアログが開いていました。
- データセットのフォルダ名を変更できるようになりました。また、フォルダ単位でまとめて表示/非表示を切り替えられるようになりました。
- 軸の目盛(目盛線本体)・目盛の数値をそれぞれ個別に表示/非表示にできるようになりました。
- ウォーターフォール(積み重ね)表示のオクルージョン(手前のトレースが奥のトレースを隠す機能)をON/OFFできるようになりました。
- データエディタが独立したタスクバー/Alt+Tabウィンドウとして表示されるようになり、メインウィンドウの背後に隠れても見つけやすくなりました。同じデータセットに対して「データ表示/編集」を再度押すと、作り直さずに最前面へ呼び戻します。

### バグ修正

- 文字列カテゴリのX軸に数値が混在するデータを読み込むとクラッシュする不具合と、それに連鎖して発生していた「Internal C++ object (QMenu) already deleted」エラーを修正しました。
- ウォーターフォール表示を適用すると、軸の枠線・目盛が隠れてしまう不具合を修正しました。
- 環境によってはmatplotlibのフォントが欠落しておりクラッシュすることがある問題への防御的対応を行いました。
- 単位変換の第2X軸で、変換後の値域によっては目盛りが極端に増えて描画が重くなる不具合を修正しました。
- オートセーブの保存先が読み取り専用フォルダの場合、オートセーブが機能しなくなる不具合を修正しました(書き込み可能な場所へ自動的にフォールバックします)。
- エクスポートプレビューのコピーボタンで、SVG形式を選ぶとクリップボードに貼り付けられない不具合を修正しました(PNG版も併せてコピーするようにしました)。
- タイトル・軸ラベル編集で、文字装飾(太字・イタリック等)を繰り返し適用すると無意味に重なっていく不具合を修正しました。
- 文字装飾ボタンを、文字を選択せずに押すと出ていた案内ポップアップを削除しました(単に何もしない仕様に変更)。
- 一部のボタンが、一度押すと次に別のボタンを押すまで色がついたままになる不具合を修正しました。
- グリッド線設定のスピンボックスで、数字が見切れる不具合を修正しました。
- 記号パレットの文字サイズを拡大し、マイナス記号(−)を追加しました。
- matplotlibツールバーのツールチップを日本語化しました。
- スクロールバーをドラッグ中の色を、他の箇所と同じ配色に統一しました。

### exe/appビルド関連

- exe/appビルドで、SVG/PDF形式でのエクスポートが失敗する不具合を修正しました(必要なmatplotlibモジュールの同梱漏れ)。
- リリース前チェックにより、ベースライン補正・区間積分・Voigtフィット・列の要約統計量など一部の機能がexe/appビルドでのみ動作しない可能性があった問題を修正しました(scipyモジュールの同梱漏れ)。

### 性能改善

- アプリの起動時間を短縮しました(約20%)。
- プロットの点数が多い状態でミニマップを操作すると重くなる不具合を修正しました。

---

## v1.3.1 (2026-08-17)

エクスポート時に間引きなしの全解像度で書き出せる新オプションと、点数の多いデータの表示・エクスポート周りで見つかった複数の不具合修正が中心のリリースです。

### 新機能: フル解像度でのエクスポート

- 「名前を付けてエクスポート」「バッチエクスポート」「エクスポートプレビュー」の3箇所に「フル解像度でエクスポート(間引きなし)」オプションを追加しました。有効にすると、通常は表示負荷軽減のため間引かれるLine(折れ線)データや2Dマップ(ヒートマップ/等高線)を、間引きなしの全データ点・全解像度で書き出せます。

### バグ修正

- Scatter(散布図)・Line+Scatterのマーカーが、表示用の間引き(点数の多いデータセットを軽量化する機能)の対象に誤って含まれており、実際のデータ密度が画面上で失われることがある不具合を修正しました。今後は連続曲線(折れ線)のみを間引き対象とします。
- 「平滑化」をScatter・棒グラフ・面グラフに適用すると、本来のマーカー・棒・塗りつぶし表示が平滑化された線に置き換わってしまう不具合を修正しました(平滑化は折れ線系のみに適用されるようになり、対象外の種類ではチェックボックス自体を隠すようにしました)。
- 平滑化を有効にした曲線をデータカーソルでクリックすると、無関係なデータ点が選択されてしまうことがある不具合を修正しました。
- ウォーターフォール(積み重ね表示)を、文字列カテゴリを使うX軸(棒グラフ等)のデータセットに適用すると、アプリがクラッシュする不具合を修正しました。
- 誤差の「帯」表示を棒グラフ・面グラフに適用すると見た目が不自然になる問題に対応し、これらのグラフ種別では選択できないようにしました。
- 点数の多いデータセットでポイントラベル(各点の値表示)と表示用間引きを同時に使うと、まれに間違った値が表示されることがある不具合を修正しました(既定設定では発生しない、環境設定でポイントラベルの上限点数を変更した場合のみの問題でした)。

---

## v1.3.0 (2026-08-17)

曲線フィット・データ解析機能の大幅な拡充と、2Dマップ(ヒートマップ/等高線)機能の新規追加が中心の大型リリースです。プロジェクトファイル形式(`.graphica`)は既存のプロジェクトファイル・自動保存ファイルと後方互換性があります。

### 曲線フィット・データ解析の拡充

- フィットパラメータの初期値・固定・範囲拘束を指定できるようになりました。
- 対応モデルにVoigt/pseudo-Voigt/Lorentzian/2成分指数関数/Boltzmannシグモイド/Hill式を追加しました。
- フィット結果に信頼帯・予測帯を重ねて描画できるようになりました。
- 残差プロット専用パネルを追加しました。
- 複数のピークが重なったデータを、N成分+ベースラインとして同時にフィットする「多峰分離フィット」を追加しました。グラフをクリックして各ピークの初期位置を指定できます。
- ベースライン補正(ALS法/多項式法/ラバーバンド法/手動点指定)を追加しました。
- 区間積分(台形則/Simpson則)、ピーク定量表(位置・高さ・半値幅・面積・重心)を追加しました。
- 複数データセットを共通のX格子へリサンプリング/補間する機能を追加しました。
- フィット結果を、再フィットなしで表・CSV・グラフ上の注釈として書き出せるようになりました。

### 2Dマップ機能(新機能)

- ヒートマップ(2Dグリッドデータの色分け表示)を追加しました。カラーマップ・値の範囲(vmin/vmax)を指定できます。
- カラーバー(位置・幅・ラベルのカスタマイズ)を追加しました。
- 測定点が格子状に揃っていない散在データも、自動補間してヒートマップ表示できます。
- 等高線図(線のみ/塗りつぶし/ヒートマップに重ね描き、の3種類)を追加しました。
- ヒートマップ・等高線図上で線をドラッグすると、その断面(1Dデータ)を新しいデータセットとして抽出できます。

### データの処理履歴・再現性

- データセットがどのような処理(演算・補正・フィット等)を経て作られたかを記録し、ツリー表示で確認できるようになりました。処理の流れを説明する文章(「方法」文)を自動生成する機能も追加しました。
- グラフをPythonスクリプトとしてエクスポートできるようになりました(matplotlibコードとして書き出され、Graphica無しでも再現・再利用できます)。
- 図の書式(タイトル・軸・カラーバー・データセットのスタイル)を独立ファイルとして保存し、他のプロジェクトに適用できるようになりました。

### 操作性・パフォーマンス向上

- 点数の多いデータセットを表示する際、見た目を保ちながら自動的に間引いて描画速度を改善しました(エクスポート画像にも同じ間引きが適用されます)。
- 複数データセットへの書式の一括適用、マウス操作(ホイールズーム等)の拡充、グラフ上でドラッグして範囲を選択しデータをマスク(除外)する機能を追加しました。
- 複数サブプロット間での軸共有(sharex/sharey)、単位変換用の第2X軸を追加しました。
- グラフの再描画・データ読み込み・バッチエクスポートの重い処理をバックグラウンドスレッド化し、操作中のアプリの応答性を改善しました。
- データセットの表示/非表示をワンクリックで切り替えられるようになりました。

---

## v1.2.1 (2026-08-13)

macOS版(Apple Silicon、未署名)の追加と、それに伴って見つかったバグ修正が中心のリリースです。

### macOS対応

- macOS版(`Graphica.app`、Apple Silicon/arm64)を初めて配布します。署名は行っていないため、初回起動時に「開発元が未確認のため開けません」という警告が表示されます。Finderで`Graphica.app`を右クリックして「開く」を選ぶと起動できます。
- 日本語のタイトル・軸ラベル・凡例が、Windows専用フォント名にしか対応していなかったためmacOSでは正しく表示されない不具合を修正しました。

### バグ修正

- 複数のグラフタブを開いた状態でダークモード/ライトモードを切り替えを繰り返すと、まれにアプリ全体が異常終了することがある問題を修正しました。

### 内部品質

- 自動テストの実行方式を改善し、CI環境での安定性を高めました(直接の機能追加ではありません)。

---

## v1.2.0 (2026-08-09)

このリリースは大きく2つの機能追加をまとめたものです。プロジェクトファイル形式(`.graphica`)は既存のプロジェクトファイル・自動保存ファイルと後方互換性があります。

### プラグイン機能の追加

- Graphicaにプラグインによる機能拡張の仕組みを追加しました。プラグインは以下を追加できます。
  - データファイルの読み込み/書き出し形式の追加(独自フォーマット対応)
  - データ処理・解析メニューへの機能追加
  - 曲線フィットに使える関数の追加
  - 独自のドックパネルの追加
- 「環境設定」→「プラグイン」タブから、読み込まれているプラグインの一覧表示・有効/無効の切り替え・エラー内容の確認ができるようになりました。
- 壊れたプラグインが読み込まれても他のプラグインやアプリ本体の動作には影響しません。起動時に問題が起きた場合は「セーフモード」で起動し、プラグインを無効化した状態で復旧できます。
- プラグイン開発者向けドキュメント(`docs/plugin_development.md`)を追加しました。

### GUIの刷新

- アプリ全体の見た目をフラットでモダンなテーマに刷新し、ダークモードもグラフ描画(matplotlib側)まで含めて全面的に対応しました。
- よく使う操作をピン留めできる「クイックアクセスツールバー」を追加しました。
- ドック(データセットプロパティ、プロット設定など)にフォーカス時のハイライト表示、角丸・枠線などの視覚的な統一感を持たせました。
- ボタン・入力欄・コンボボックス・ダイアログ・アイコンセットなど、UI全体の細部を実機フィードバックをもとに調整しました。
- タイトル/軸ラベルの編集を、その場でmathtext記法のプレビューを見ながら編集できる専用ダイアログに変更しました。

### バグ修正

上記の開発過程で、以下を含む複数の不具合を修正しました。

- Undo(元に戻す)操作の一部で、無関係な行のデータが破損する可能性があった問題
- バックグラウンドでのファイル読み込み中にウィンドウを閉じるとアプリ全体がクラッシュする可能性があった問題
- 複数タブを開いている状態でダークモードを切り替えると、他のタブの表示が古いままになる問題
- ピーク検出(谷)で、負のしきい値を指定した場合に意図と異なる点が検出される問題
- グラフのサブプロット数を減らした際、該当するデータが描画・書き出しから消えてしまう問題

### 内部品質

- 画像回帰テストの追加、自動テストの大幅な拡充(カバレッジ約70%→97%)など、今後の開発の安全性を高める変更を行いました。ユーザーが直接触れる機能追加ではありませんが、今後のリリースの安定性に寄与します。

---

## v1.1.0 以前

このファイルの整備前のバージョンについては、Gitのコミット履歴を参照してください。
